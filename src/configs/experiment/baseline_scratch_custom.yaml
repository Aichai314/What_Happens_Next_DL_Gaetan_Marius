# @package _global_
# Baseline experiment: ResNet18 + temporal average pooling
defaults:
  - override /model: cnn_baseline

model:
  pretrained: false

training:
  epochs: 5
  batch_size: 128 # Should be just the limit
  num_workers: 4