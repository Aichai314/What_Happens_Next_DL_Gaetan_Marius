# @package _global_
model:
  name: tsm_baseline
  pretrained: false
  num_classes: ${num_classes}
  dropout: 0.0 # No dropout in the baseline
  model_size: 34
  split_tdm: true # Use SplitTDM instead of regular TemporalDifference by default